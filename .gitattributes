# Auto detect text files and perform LF normalization
* text=auto

# Treat notebooks as documentation/ report, not source code, so GitHub's language treat them as a markdown
# instead of being dominated by the notebook's byte size

# *.ipynb linguist-documentation
